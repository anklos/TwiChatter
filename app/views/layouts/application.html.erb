<!DOCTYPE html>
<html>
  <head>
    <title>Twitter Chatter</title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag :defaults, "http://vivid-sword-660.heroku.com/bayeux.js" %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
    <div>
      <% if current_user %>
        Welcome <%= current_user.name %> | <%= link_to "Sign out", signout_path %>
        <div id="username"><%= current_user.nickname %></div>
        <div id="container">                  
          <%= yield %>
        </div>
      <%else%>      
      <%= link_to 'Sign in with Twitter', "/auth/twitter" %>
      <br/>
      <h2>Please dont be afraid to sign in, this demo will only get read access to your twitter account</h2>
      <%end%>
    </div>    
  </body>
</html>
